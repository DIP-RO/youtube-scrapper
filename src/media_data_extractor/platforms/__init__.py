"""Platform implementations for media-data-extractor.

Each platform (YouTube, TikTok, etc.) has its own subpackage with:
- ``scraper.py`` — main scraper class
- ``parser.py`` — platform-specific parsing
- ``downloader.py`` — media download support
"""
